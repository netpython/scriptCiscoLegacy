# Programmes avancés

Ces cinq programmes produisent des audits de conformité complets : sécurité IOS, dérive de configuration, capacité des interfaces, topologie/redondance et standard logiciel majoritaire.

Ils utilisent le même `inventory.yml` et les mêmes variables `CISCO_USERNAME`, `CISCO_PASSWORD` et `CISCO_SECRET` que les scripts rapides.
